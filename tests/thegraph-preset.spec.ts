import preset from '../src/index';

describe('thegraph-preset', () => {
    it('creates documents', async () => {
        preset.buildGeneratesSection({
            
        })
    });

    it('attaches typescript type resolution to codegen when appropriate', () => {

    });
});